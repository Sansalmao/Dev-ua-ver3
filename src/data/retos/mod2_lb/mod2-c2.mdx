---
moduleId: "mod2"
challengeId: "c2"
moduleTitle: "Module 2 – Unicode Advanced Programming"
moduleDesc: "Expands understanding of Unicode: character-glyph model, normalization, character database, string comparison, and bidirectional scripts."
exerciseNum: "Exercise 2"
exerciseTitle: "Develop a simple code in Java that verifies if two Unicode strings are equal or not equal, considering case‑insensitive comparison."
exerciseItems:
  - "Read two strings from the user."
  - "Use `String.equalsIgnoreCase()` to compare them."
  - "Print whether the strings are equal or not equal (case‑insensitively)."
solutionLang: "java"
solutionCode: |
  import java.util.Scanner;

  public class Mod2_Exercise2 {
      public static void main(String[] args) {
          Scanner scanner = new Scanner(System.in);

          System.out.print("Enter the first string: ");
          String firstString = scanner.nextLine();

          System.out.print("Enter the second string: ");
          String secondString = scanner.nextLine();

          boolean result = firstString.equalsIgnoreCase(secondString);

          System.out.println("\n--- Results ---");
          if (result) {
              System.out.println("The strings are EQUAL (case-insensitive comparison)");
          } else {
              System.out.println("The strings are NOT EQUAL (case-insensitive comparison)");
          }

          scanner.close();
      }
  }
draft: false
---
